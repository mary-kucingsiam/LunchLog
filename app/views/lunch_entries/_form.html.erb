<%= form_with(model: lunch_entry, local: true) do |form| %>
  <% if lunch_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lunch_entry.errors.count, "error") %> prohibited this lunch_entry from being saved:</h2>

      <ul>
        <% lunch_entry.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :food_name %>
    <%= form.text_field :food_name %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
